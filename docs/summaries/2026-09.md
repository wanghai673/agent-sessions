# September 2026

- Added an independent Codex App opening action, using the canonical thread UUID and `codex://threads/<id>?hostId=local` with the `com.openai.codex` application. Terminal resume remains available. Tests cover link validation, application lookup/launch failures, and CLI-independent routing. A preview build passed the full Xcode suite and the user confirmed navigation to the existing conversation; writer conflicts remain enforced by Codex.
- Extended working-task discovery to the bundled Codex desktop backend, including renamed app installations. Each writable rollout is a separate candidate instead of collapsing a shared process to one thread. Cached, bounded lifecycle-event reads publish only unfinished turns; completion or interruption removes the marker even when the backend retains the file. Idle and unknown-state desktop candidates are not labeled as open, because the rollout does not identify the visible conversation. Regression tests cover retained-history false positives, concurrent turns, completion/restart between process scans, unchanged CLI idle state, partial writes, and cache invalidation.

- Fixed Quota Meter Codex runway discovery to use the configured sessions directory, including `SessionsRootOverride`, process-inherited `CODEX_HOME`, and the default path, instead of a separate hard-coded root. Weekly calibration account scoping and OAuth auth-file lookup now use the same effective Codex home; GUI launches that do not inherit a shell-only `CODEX_HOME` should use the Preferences override.

- Closed four DeepSeek Harness pre-merge review findings: propagated migrated seed boundaries, kept Saved archives monotonic through re-star, excluded injected plugin messages from search text, and derived interruption markers from normalized turn events.

- Prepared the unreleased 5.5 DeepSeek Harness source integration: local read-only browsing/search over supported v0-v3 plain JSONL and independently framed, checksum-validated Zstandard generations, immutable directory-generation discovery, and catalog-gated known-event dispositions. Incomplete or corrupt artifacts stay invisible; seeded forks remain roots with provenance; DSH archive state, resume, live activity, telemetry, attachment dereferencing, and archive-state mirroring remain out of scope. Compatibility evidence comes from sanitized fixtures, the pinned event catalog, and a local manual UI smoke test; broad real-world coverage remains unverified.

- Reduced large-library session-list refresh cost by retaining a bounded working set for live-presence joins and removing the duplicate table rebuild that ran when indexing finished. The list's published rows remain the source of truth for refresh completion, preserving selection reconciliation without redoing the hierarchy build.

- Increased the compact Quota Meter's inter-column spacing so the weekly percentage and aggregate burn rate remain visually distinct at the standard text size.

- Added an adaptive Quota Meter header for the weekly Session Runway lens: it combines measurable active-session `%/h`, projects the remaining weekly time at that pace with readable hour/minute spacing, summarizes projections longer than one day as a whole-hour lower bound, retains the weekly reset, and renders an absent five-hour limit consistently as `∞`. Active rates report measuring, unavailable, or exhausted states when needed; with no active session, the rate and ETA area remains empty.

- Added Cline CLI and Cline Desktop as one local session source, with shared-format discovery, transcript parsing, search, source labels, Preferences controls, saved-session backfill, redacted real-schema fixtures, weekly format monitoring, and explicit unsupported boundaries for resume, live status, images, hierarchy, and telemetry. Invalid custom-root drafts no longer replace the working root, `CLINE_DATA_DIR` remains authoritative, custom binary availability follows the last successfully probed executable, and a path change during a version check cannot publish stale probe state. Missing or partial companion files preserve the current transcript, full loads remain size-bounded, and monitoring validates both files' version, identity, and logical freshness.

- Replaced OpenCode's timestamp-only default session names with a capped one-line first-prompt fallback during source parsing. The current SQLite and legacy JSON paths now agree, while explicit names and subagent titles remain authoritative and the shared session-row hot path stays unchanged.

- Preserved Droid's explicit session-store title during full transcript parsing, keeping list display and title search stable after search prewarming or other wholesale hydration paths.

- Kept OpenClaw's derived Telegram and tool-fallback titles consistent between lightweight and full parsing, preventing search hydration from replacing recognizable rows with raw message text.

- Preserved Claude's cleaned prompt title through full transcript parsing and search prewarming, so local-command Caveat wrappers do not replace recognizable session rows.

- Hardened session retrieval after 5.3: project filters now use path-backed identity instead of substring names, displayed-title matches supplement stale or capacity-limited full-text results, and nonempty searches rerun when the underlying session membership changes while keeping the previous rows visible until replacement results arrive.

- Reconciled Antigravity's cached startup rows against authoritative roots, preserving cache data for unavailable roots while applying confirmed additions, changes, and deletions. Codex state fallback titles now remove complete injected context and pasted-file wrappers while preserving explicit titles, malformed wrappers, and ordinary user text.

- Added localized in-app What's New content for 5.3, highlighting Session info and the calmer toolbar in English and Simplified Chinese.

- Added Shift-Command-F as a global Search Sessions alias while retaining the existing Option-Command-F shortcut.

- Made Session info's core figures literal and easier to scan: model and thinking effort are separate, the compact API-equivalent label keeps its explanatory estimate tooltip and sub-cent precision, the token bar shows shares without losing sub-percent values and exposes them to VoiceOver, and transcript span names its wall-clock meaning. The header records the last manual refresh; configuration history appears only for recorded changes; unavailable weekly provenance remains hidden.

- Hid Session info's Weekly quota row until Codex records a durable session account identity that can safely match the account-wide weekly calibration; current transcripts leave it permanently unavailable.

- Added per-session activity to Session info and cut standing text from both footers. Span and turn composition come from the transcript's own records; no cross-session aggregate is involved, and the span is explicitly wall clock rather than an invented active-versus-idle split, because neither provider records request durations.

- Reduced toolbar weight across the window: the main toolbar keeps source filters, search, Quota Meter, Resume, Image Browser, and transcript and Session info controls, while lower-frequency actions move into an overflow menu. The transcript toolbar collapses from two rows to one with the rarely used view-mode picker demoted to that menu; Shift-Command-T still cycles modes and source shortcuts remain unchanged.

- Unified the window's surfaces behind explicit chrome and paper tokens. The transcript is the only lifted surface; the session list, toolbars, and Session info inspector now share one level, which fixes the white-inspector-beside-warm-transcript mismatch that only showed in light mode.

- Reduced the search spike caused by large-transcript tokenization and live refreshes. Common one-term queries scan token boundaries without allocating strings for every unrelated word, and a focused Codex session consumes only complete JSONL records appended after its stable full hydration. Truncation, replacement, and unsafe cursor states still fall back to the correctness-preserving full parse, while per-session cache coalescing prevents overlapping full-history renders during rapid appends. Presence polling reuses Claude Desktop sidecar results for ten seconds, removing the full-tree walk that previously overlapped interactive searches every two seconds.

- Removed the Session info CPU regression from the main window: a hidden inspector no longer re-reads each selected transcript, canceled selection scans stop promptly, and Codex and Claude telemetry skip JSON decoding for records that cannot contain usage or configuration data.

- Restored Claude's dollar and calibrated weekly Session Runway rates for real transcripts, which report inference geography as unavailable or empty. Those provider states use published standard rates; explicit US inference retains regional pricing and malformed values remain unavailable. Live and cached readings from one Claude endpoint now preserve the same weekly calibration instead of restarting its scan, while the account-unscoped bootstrap stays confined to its current process and quota window.

- Kept Codex subagent trees stable while opening a transcript. Both the instant tail preview and the completed parse preserve the parent's raw runtime identity, preventing children from becoming roots and triggering a session-list rebuild that loses the selected row and scroll position.

- Stabilized Codex Session Runway across slow bootstrap scans and OAuth/CLI-RPC fallback. Account-less local history may power the live estimate but remains barred from historical account attribution, while cancellation and Auto visibility no longer turn a refresh into disappearing rows.

- Added Session info to the existing transcript pane without changing the sessions list or split arrangement. The inspector and inline configuration markers preserve per-session and subagent telemetry ownership, with explicit refresh and no background polling.

- Rebuilt the Session info hierarchy around one headline number: API-equivalent cost leads, the token total sits beneath it with a cached/fresh/output share bar, and provenance moved into a collapsed "How this was estimated" group. Configuration history became a clickable timeline that scrolls the transcript to the matching inline marker, and the pricing basis now groups by model, speed, and region instead of emitting a row per request. Runtime-localized labels, legends, history, accessibility text, and whole plural sentences keep the full inspector coherent in English and Simplified Chinese.

- Hardened transcript telemetry and its quota calculations: exact manifest fingerprints survive into cached results and events, Codex request/account evidence must match completely across account switches, cache writes remain distinct from fresh input and require a published rate, and malformed Claude speeds, inference regions, or model versions fail closed. Added Claude 4.6+ US-region pricing and corrected historical Opus 4, Fable/Mythos 5.1 cache-read, and GPT-5.5/GPT-5.4 long-context pricing.

- Updated the temporarily hosted Session-Bench page from its standalone repository: O3 cells link to checked vendor sources and show source-check dates, while scores and ranks remain unchanged. Agent Sessions now retains only the Jekyll view and a byte-identical generated leaderboard instead of a second benchmark engine.

- Cataloged the weekly burn estimate tooltip in English and Simplified Chinese, removed the unused “Dismiss for now” entry, and updated the localization key baseline to resolve CI coverage and extraction drift failures.
- Closed four format-sweep review gaps: newest prebump outcomes now win across success and drift, Claude's test exercises the actual page-join loop, Kimi ignores redacted zero timestamps, and Copilot's latent chunk-coalescing risk is filed in the backlog.

- Prepared the 5.2 release surface with in-app What's New content, a concise README summary, current Quota Meter imagery, and a stricter test-count baseline.
- Reworked the README and GitHub Pages entry points around finding old agent sessions, aligned the 15-source capability claims, removed the outdated quota animation, and shortened the path from product proof to download.
- Corrected Cursor's public support label to Agents Window and CLI sessions, with the guide now stating that DB-only IDE sidepane chats are not decoded.
- Reframed the Codex and Claude guides around finding an old session and added aggregate download/GitHub exit measurement to the existing website analytics.
- Closed the steward-check false-green path: a matching fingerprint can no longer override stale or thin evidence, a failed discovery contract, or a scope-none compatibility verdict.
- Fixed Claude usage fallback when plugin content moves the weekly quota below the first terminal page; the bounded capture now scrolls through the view and returns both current-session and all-models weekly limits.
- Added a targeted session-list card inviting established users whose preferred macOS language is not yet supported to contribute a translation; bundle-derived locale coverage, bounded retries, and local-only targeting keep it current and non-nagging.
- Unified all seven session-list cards on one adaptive layout and launch-stable priority decision made only after every active source reaches ready or error. Different campaigns are separated by five quiet days; the Star, translation, and add-an-agent session gates are staggered at 25, 40, and 60. Every bounded lifecycle advances at most once per launch when a final impression and soft action coincide, × means never ask again, and retention counts distinct locally hashed sessions instead of repeated selections.
- Clarified optional sample reporting for every monitored agent: generated, withheld, unavailable, and disabled are distinct outcomes, with no change to format verdicts or redaction rules.
- Added GPT-6 Astra to bundled and hosted runway pricing, restoring dollar and calibrated weekly burn for Astra sessions; covered rates, long-context/fast tiers, and rejection of stale pre-Astra manifests.

- Published a reusable social-banner pack on the Agent Sessions site, with X, Open Graph/GitHub, LinkedIn, and 16:9 exports of the approved native composition.
- Expanded Simplified Chinese localization from the initial catalogs to the remaining user-visible surfaces, adding typed localization boundaries for Settings, Quota Meter, session/transcript controls, analytics, AppKit menus and alerts while preserving raw transcripts, paths, commands, IDs, and product/provider names.
- Quota Meter's `Wk` rate now excludes Codex's per-request usage twin from cumulative-counter differencing, retains completed sessions for the full measurement window, recovers that history across oversized image/tool records, and uses a linearly decayed five-minute window with a one-minute evidence floor instead of extrapolating and holding a short peak; calibration is weekly-anchor scoped, uses event time rather than discovery time, baselines anchor transitions, rejects malformed, incomplete, contradictory, post-cutoff, or unresolved long-context evidence, and invalidates older accounting caches. Overlapping refreshes cannot move the poll clock backward. Sol long-context requests use their published price tier, unknown GPT suffixes fail closed, and displayed rates keep the plain form `7.1%/h`, with estimation context in the tooltip.
- Restored the Quota Meter toolbar's Wk/Hr/$/tk Session Runway selector at its normal width by removing the lower-priority font-size shortcut; Standard and Enlarged remain available in Quota Meter Settings.
- Established the localization foundation for the planned Simplified Chinese contribution: English app and Info.plist catalogs, stable identifiers independent of translated display text, typed localizable-versus-verbatim boundaries, pluralized session counts, locale-aware notification durations, and CI validation that rejects partial locale imports and catalog/source drift.
- Quota Meter now turns persistent Claude billing/account-access failures into an actionable `Claude plan inactive` state after 90 seconds, with a one-shot notification and inline Hide action; long server-directed rate limits stay on `rate limited · check plan` with help noting that the subscription may be inactive, even when a fallback fails, and stale cold-start work cannot replace the server retry schedule, while scope-only 403 responses remain eligible for usage fallbacks.
- Codex and Claude can be shown or hidden independently from Quota Meter or Usage Tracking settings; hiding changes only Quota Meter presentation and leaves tracking, footer/menu-bar data, and session indexing intact.
- Antigravity and OpenCode brand colors are pinned instead of inheriting OS-dependent AppKit palette values, keeping their accents and toolbar pills stable across macOS 15 and 26.
- Post-5.1.1 engineering work added the provider-neutral telemetry foundation for Codex, Claude, Pi and Copilot, including configuration-change history, request-level self/descendant token ownership, immutable price provenance, long-context pricing, and conservative weekly-quota attribution from scoped raw observations; normalized identity-backed metadata timestamps without deleting indexed rows; isolated test runs from the live application index; and taught the FX steward check to export redacted sidecar drift safely.
